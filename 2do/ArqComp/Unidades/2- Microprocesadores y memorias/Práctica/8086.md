Pregunta de final: Explicar como es la estructura de una computadora en bloques
    Bus de sistema (B1)
    Micro (B2) -> CU, ALU, Reg internos, otras funcionalidades, niveles de caché. TOdo se conecta por buses internos
    Mem principal (B3) -> Programas, datos, E/S
    Bus E/S -> interfaces y disp E/S
VER IMAGEN DEL LIBRO DE PATRICIA QUIROGA

### Registros generales del 8086 (PREGUNTA DE FINAL)
* AX: AH (8 a 15), AL (0 a 7)

### DOSBox
Copiar el debug.exe en la caprte de dosbox (la que se montado)

En DEBUG.EXE en -d 100 cada renglon son 16 bytes? en little endian

Cod op mas chico: 1 byte

6 byts de hexa * 4 bytes de peso = 24 bytes/8bits = 3 bytes de memoria que ocupa la línea de código.
4 bytes de hexa * 4 bytes de peso = 16 / 8bits = 2 bytes de memoria que ocupa la línea de código.

DS, ES, SS, CS tienen misma dirección de base de segmento, son 64kB para los cuatro registros. SOn de 64kB porque los archivos .COM no pueden direccionar más que eso.

### DIFERENCIA ENTRE DIRECCIÓN LÓGICA Y DIRECCIÓN EFECTIVA DE MEMORIA
Dir lógica: 

Dir ef mem:
